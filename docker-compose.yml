version: '3.8'

services:
  nexus-banking-app:
    container_name: nexus-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Maps your machine's port 8080 to the Nginx internal port 80
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
